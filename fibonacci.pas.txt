program Fibonacci;
var
  number, i, a, b, temp: integer;
begin
  write('Enter sequence length: ');
  readln(number);
  
  a := 0;
  b := 1;
  write('Fibonacci: ', a, ' ', b, ' ');
  
  for i := 3 to number do
  begin
    temp := a + b;
    write(temp, ' ');
    a := b;
    b := temp;
  end;
end.